version: '3.8'

services:
  team-hub:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      # This anonymous volume prevents your broken Windows node_modules from overriding the Linux ones in the container
      - /app/node_modules 
    environment:
      - PORT=3000
